<?php View::flash(); ?>
<div class="w3-container">
   <?= View::Encab($controller_name, $page_action, $breadcrumb); ?>
   <h2> <?="<b>$asignatura</b> -- <b>$grado [°$grado->id]</b>"?> </h2>
   <?php
      $Tabs = new OdaTabs();
      foreach ($data as $periodo => $regs_periodo) {
         $tabla = new OdaTable();
         $lnk_button_new = _Tag::linkButton('docentes/indicadores/edit/edit', "Nuevo P$periodo").'<br>';
         foreach ($regs_periodo as $key => $indic) {
            $cod = '<u>'.$indic->codigo.' '.$indic->valorativo.'</u><br>';
            $datos=array($cod.$indic->concepto);
            $tabla->setBody($datos);
         }
         $Tabs->addTab("p$periodo", "Periodo $periodo", $lnk_button_new.$tabla);
      }
      echo $Tabs;
   ?>
</div>

<?= tag::js("tabs_h", false);?>